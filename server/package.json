{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "server": "json-server --watch db.json --port 5000 --static ./public"
  },
  "dependencies": {
    "dotenv": "^16.0.2",
    "json-server": "^0.17.0"
  }
}
